<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
    "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">

<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta name="generator" content=
  "HTML Tidy for Linux (vers 25 March 2009), see www.w3.org" />

  <title></title>
</head>

<body>
  <!--CARDS SECTION -->
  <div class="album py-5 bg-light" style="margin-top: 2%">
    <div class="container">
      <div class="row">
        <% @post.each do |post| %>
        <div class="col-md-4">
          <div class="card mb-4 box-shadow">
            <div class="card-body">
              <h5 class="card-title"><%= link_to post.title, post %></h5>
              <p class="card-text" style="white-space: nowrap; overflow: hidden; text-overflow: ellipsis;"><%= post.body %></p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <%= link_to "View", post_path(post), :class => 'btn btn-sm btn-outline-secondary' %> 
                </div>
                <small class="text-muted">Created <%= time_ago_in_words(post.created_at) %></small>
              </div>
            </div>
          </div>
        </div>
        <% end %>
      </div>
      <!-- BEGIN PAGINATION TOOL -->
      <%= will_paginate @post, previous_label: "Older", next_label: "Newer", inner_window: 1, outer_window: 0 %>
      <!-- END PAGIATION TOOL -->
    </div>
  </div>
<!--END OF CARDS SECTION-->
</body>

</html>

 
  



